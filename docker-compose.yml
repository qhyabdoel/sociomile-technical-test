services:
  db:
    image: mysql:latest
    container_name: sociomile_db
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: sociomile_db
    ports:
      - "3306:3306"
    volumes:
      - ./backend/migrations:/docker-entrypoint-initdb.d
